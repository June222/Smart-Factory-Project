import 'package:flutter/material.dart';

class GraphModeScreen extends StatelessWidget {
  const GraphModeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
