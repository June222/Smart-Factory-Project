import 'package:flutter/material.dart';

var introductionPadding =
    const EdgeInsets.symmetric(horizontal: 20, vertical: 10);

var headLineStyle =
    const TextStyle(fontWeight: FontWeight.bold, fontSize: 30, height: 2.0);
var boldStyle = const TextStyle(fontWeight: FontWeight.bold, height: 1.7);
